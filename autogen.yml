name: LibZip
cflags: '-I/usr/local/include'
ldflags: '-lzip'
destdir: src/zip/
definitions:
  libzip:
    description: 'LibZip binding'
    includes: ['zip.h', 'zipconf.h']
    prefixes: ['zip_', 'ZIP_', 'Zip_']

options:
  import_docstrings: full
